# BENCHMARK

A quarterly AI-company strategy game spanning Q1 2020 through Q1 2027.

```powershell
npm.cmd install
npm.cmd start
```

Then visit `http://127.0.0.1:4173`. Progress and named saves are stored locally in the browser. Standard and Hard Mode are both included without payment.
